# Clerk Authentication
# Copy this file to .env and add your Clerk Publishable Key
# DO NOT commit .env to version control!
# PRODUCTION: use pk_live_* (not pk_test_*)
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_KEY_HERE

# Supabase
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Expo Push (optional — avoids rate limits)
EXPO_ACCESS_TOKEN=your_expo_access_token_here

# Google Gemini (AI Assistant)
# PRODUCTION: use a key with appropriate quotas from https://aistudio.google.com/apikey
EXPO_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# --- Payments (Edge Function "payments-initiate") ----------------------
# These are server-side secrets — set them on the Supabase Edge Function
# environment, NOT in the mobile bundle. When unset, the Edge Function
# falls back to "mock mode" which auto-succeeds payments after 3 s (dev
# only). To force mock mode in prod, set PAYMENTS_MOCK_MODE=true.

# Singpay (Airtel Money + Moov Money aggregator) — Gabon
SINGPAY_API_URL=https://api.singpay.ga/v1
SINGPAY_API_KEY=your_singpay_api_key_here
SINGPAY_WEBHOOK_SECRET=your_singpay_webhook_secret_here

# PayPal REST API (sandbox vs live)
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_SECRET=your_paypal_secret_here
PAYPAL_BASE=https://api-m.sandbox.paypal.com  # use https://api-m.paypal.com in prod

# Auto-succeed toggle: 'auto' (succeed when provider keys are missing —
# the test-phase default) | 'true' (always succeed, ignores providers) |
# 'false' (never auto-succeed, requires real provider creds to settle).
PAYMENTS_AUTO_SUCCEED=auto

# Resend (transactional email — sends the payment receipt to the payer).
# Without RESEND_API_KEY the Edge Function logs the receipt body to its
# function logs instead, and still marks `receipt_sent_at`. Add the key
# to switch to real delivery.
RESEND_API_KEY=re_your_resend_key_here
RESEND_FROM=ZopGo <onboarding@resend.dev>  # change to a verified domain in prod
